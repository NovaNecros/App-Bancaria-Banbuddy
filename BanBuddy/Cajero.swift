import SwiftUI

struct Cajero : View
{
    var body : some View
    {
        VStack(alignment: .center, spacing: 0)
        {
            
        }
        .navigationBarBackButtonHidden(true)
    }
}
